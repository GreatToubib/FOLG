function [accuracy ] = validation( Xtest, Ytest, W_trained )
%UNTITLED6 Summary of this function goes here
%   Detailed explanation goes here
    % validation 
count=0;
for test_sample = 1:p
    [m,p] = size(Xtest);
    ypred = W_trained' * Xtest (:,test_sample) ; 
    a_ypred = sigmoidFunction(ypred);  % ypred avec activation sigmoid
    [M,I] = max(a_ypred);
    [M,I] = max(a_ypred);
    ytrue = Ytest(test_sample); % visualisation du label du test sample
    if ytrue== I 
        count= count+1;
    end
end
count
accuracy=count/p

end

